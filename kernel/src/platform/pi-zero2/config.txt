# Raspberry Pi Zero 2 W Configuration
# Required for bare-metal kernel

# Boot in 64-bit mode (AArch64)
arm_64bit=1

# GPU memory allocation
gpu_mem=64

# Enable HDMI output even without connected display
hdmi_force_hotplug=1

# Set HDMI mode (1280x720 @ 60Hz)
hdmi_group=1
hdmi_mode=4

# Disable overscan
disable_overscan=1

# Use kernel8.img as the kernel
kernel=kernel8.img
